import { Component } from '@angular/core';

@Component({
  selector: 'app-liste',
  templateUrl: './liste.component.html',
  styleUrls: ['./liste.component.scss']
})
export class ListeComponent 
{

  /*
const obj = JSON.stringify(this.inscriptionForm.value);
    
    const httpOptions = 
    {
      headers: new HttpHeaders
      (
        {
        'Content-Type': 'application/json'
        }
      )
    };
    console.log(obj);
    
    this.http.post(`http://localhost:3000/api/register`, obj, httpOptions).subscribe
    (
      (reponse) => 
      {
        console.log(reponse)
      }
    ) ,
    
    (        error: any) => {
        console.error(error); // affiche l'erreur en cas d'échec de la requête
      }


  */






/*
const obj = JSON.stringify(this.loginForm.value);
    
    const httpOptions = 
    {
      headers: new HttpHeaders
      (
        {
        'Content-Type': 'application/json'
        }
      )
    };
    console.log(obj);
    
    this.http.post(`http://localhost:3000/api/login`, obj, httpOptions).subscribe
    (
      (reponse) => 
      {
        console.log(reponse)
      }
    ) ,
    
      (        error: any) => {
          console.error(error); // affiche l'erreur en cas d'échec de la requête
        }
        */
}
